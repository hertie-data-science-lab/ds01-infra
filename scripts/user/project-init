#!/bin/bash
# Project init - Alias to new-project command
# Creates new data science projects with interactive setup

set -e

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Call new-project with all arguments
exec "$SCRIPT_DIR/new-project" "$@"
