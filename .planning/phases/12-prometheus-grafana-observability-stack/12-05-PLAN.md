---
phase: 12-prometheus-grafana-observability-stack
plan: 05
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - monitoring/grafana/provisioning/dashboards/dashboards/ds01_user.json
autonomous: true
requirements: [DASH-02]

must_haves:
  truths:
    - "User dashboard has a user variable dropdown for filtering all panels to a specific user"
    - "User dashboard shows current GPU allocations, running containers, and GPU utilisation for selected user"
    - "User dashboard shows GPU-hours consumed this week and month from recording rules"
    - "User dashboard shows quota usage (memory, GPU slots, container count) vs limits"
  artifacts:
    - path: "monitoring/grafana/provisioning/dashboards/dashboards/ds01_user.json"
      provides: "Per-user self-service dashboard with variable-based filtering"
      min_lines: 200
  key_links:
    - from: "monitoring/grafana/provisioning/dashboards/dashboards/ds01_user.json"
      to: "ds01_gpu_allocated"
      via: "PromQL with user label filter"
      pattern: 'user=.*\\$user'
---

<objective>
Rebuild the user self-service dashboard with per-user variable filtering. Users can see their own GPU allocations, utilisation, GPU-hours, and quota usage. This is the "My Status" view complementing the CLI tools (check-limits, ds01-status).

Purpose: Users need a visual dashboard to understand their resource usage and plan workloads.
Output: Rebuilt ds01_user.json with user variable and filtered panels.
</objective>

<execution_context>
@/home/datasciencelab/.claude/get-shit-done/workflows/execute-plan.md
@/home/datasciencelab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-prometheus-grafana-observability-stack/12-RESEARCH.md
@monitoring/grafana/provisioning/dashboards/dashboards/ds01_user.json
@monitoring/prometheus/rules/ds01_recording.yml
@.planning/phases/12-prometheus-grafana-observability-stack/12-01-SUMMARY.md
@.planning/phases/12-prometheus-grafana-observability-stack/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild user self-service dashboard with variable filtering</name>
  <files>monitoring/grafana/provisioning/dashboards/dashboards/ds01_user.json</files>
  <action>
  Complete rebuild of ds01_user.json. This is the user-facing dashboard — simpler than admin overview, focused on "my stuff".

  **Dashboard metadata:**
  - uid: `ds01-user`
  - title: `DS01 My Status`
  - tags: `["ds01", "user"]`
  - timezone: `utc`
  - refresh: `1m`
  - time range: last 6 hours

  **Template variable:**
  ```json
  "templating": {
    "list": [
      {
        "name": "user",
        "type": "query",
        "datasource": {"type": "prometheus", "uid": "prometheus"},
        "query": "label_values(ds01_gpu_allocated, user)",
        "refresh": 2,
        "sort": 1,
        "includeAll": false,
        "current": {},
        "label": "User"
      }
    ]
  }
  ```

  **Row 1 — My Current Status (stat panels, h=4):**

  Panel 1: My GPU Allocations
  - Query: `count(ds01_gpu_allocated{user="$user"}) or vector(0)`
  - Type: stat
  - Colour: blue

  Panel 2: My Running Containers
  - Query: `ds01_user_containers_count{user="$user"}`
  - Type: stat

  Panel 3: My GPU Utilisation
  - Query: `ds01:user_gpu_utilization_avg{user="$user"}`
  - Type: gauge
  - Unit: percent
  - Thresholds: green >50, yellow 10-50, red <10 (low utilisation = waste)

  Panel 4: My SSH Sessions
  - Query: `ds01_ssh_sessions_active{user="$user"}`
  - Type: stat

  **Row 2 — My GPU Usage Over Time (h=8):**

  Panel 5: My GPU Utilisation (time series)
  - Query: `ds01:user_gpu_utilization_avg{user="$user"}`
  - Type: timeseries
  - Fill opacity: 20

  Panel 6: My GPU Memory Usage
  - Query: `ds01:user_gpu_memory_percent{user="$user"}`
  - Type: timeseries
  - Unit: percent

  **Row 3 — My GPU-Hours (h=6):**

  Panel 7: GPU-Hours This Week
  - Query: `increase(ds01:user_gpu_seconds{user="$user"}[7d]) / 3600`
  - Type: stat
  - Unit: hours
  - Decimals: 1

  Panel 8: GPU-Hours This Month
  - Query: `increase(ds01:user_gpu_seconds{user="$user"}[30d]) / 3600`
  - Type: stat
  - Unit: hours
  - Decimals: 1

  Panel 9: My Efficiency Score
  - Query A (weighted hours): `increase(ds01:user_gpu_seconds_weighted{user="$user"}[7d]) / 3600`
  - Query B (raw hours): `increase(ds01:user_gpu_seconds{user="$user"}[7d]) / 3600`
  - Show as percentage: A/B * 100
  - Type: gauge
  - Unit: percent
  - Thresholds: green >50, yellow 20-50, red <20

  Panel 10: GPU-Hours by Day (bar chart)
  - Query: `increase(ds01:user_gpu_seconds{user="$user"}[1d]) / 3600`
  - Type: barchart
  - Interval: 1d

  **Row 4 — My Quota (h=6):**

  Panel 11: MIG Slots Used vs Quota
  - Query A (used): `ds01_user_mig_allocated{user="$user"}`
  - Query B (limit): `ds01_user_max_mig_instances{user="$user"}` (if this metric exists from exporter, otherwise omit)
  - Type: bargauge
  - Note: If max_mig_instances metric is not exposed by exporter, show just current allocation as stat panel with a note that limits are visible via `check-limits` CLI command.

  Panel 12: My Container Details (table)
  - Query: `ds01_gpu_allocated{user="$user"}`
  - Type: table
  - Show columns: gpu_slot, container, interface
  - Transform: labels to fields

  **Build as valid Grafana JSON.** All queries filter by `user="$user"`. Datasource uid: `prometheus`.
  </action>
  <verify>
    <automated>cd /opt/ds01-infra && python3 -c "
import json
d = json.load(open('monitoring/grafana/provisioning/dashboards/dashboards/ds01_user.json'))
assert d.get('uid') == 'ds01-user', 'wrong uid'

# Check template variable exists
templating = d.get('templating', {}).get('list', [])
user_vars = [v for v in templating if v.get('name') == 'user']
assert len(user_vars) == 1, 'missing user template variable'

# Check panels filter by user
panels = [p for p in d.get('panels', []) if p.get('type') != 'row']
assert len(panels) >= 8, f'expected 8+ panels, got {len(panels)}'

# Check that queries reference \$user
user_filtered = 0
for p in d.get('panels', []):
    for t in p.get('targets', []):
        expr = t.get('expr', '')
        if '\\$user' in expr or '\$user' in expr:
            user_filtered += 1
assert user_filtered >= 5, f'expected 5+ panels with user filter, got {user_filtered}'
print(f'User dashboard valid: {len(panels)} panels, user variable present, {user_filtered} panels filter by user')
"</automated>
  </verify>
  <done>User self-service dashboard rebuilt with user variable dropdown. All panels filter by selected user. Shows current status (GPU allocations, containers, utilisation), usage history (time series), GPU-hours consumption (week/month), efficiency score, and container details table.</done>
</task>

</tasks>

<verification>
- ds01_user.json is valid JSON
- Dashboard uid is `ds01-user`
- Template variable `user` exists with query `label_values(ds01_gpu_allocated, user)`
- 8+ data panels present
- Majority of panels filter by `user="$user"`
- GPU-hours queries use `increase(ds01:user_gpu_seconds{user="$user"}[...])`
</verification>

<success_criteria>
User self-service dashboard provides per-user view of GPU allocations, utilisation, GPU-hours consumed, and efficiency. Users can select their name from dropdown and see all their metrics. Dashboard complements CLI tools (check-limits, ds01-status) with visual exploration.
</success_criteria>

<output>
After completion, create `.planning/phases/12-prometheus-grafana-observability-stack/12-05-SUMMARY.md`
</output>
