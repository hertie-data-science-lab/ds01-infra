---
phase: 12-prometheus-grafana-observability-stack
plan: 04
type: execute
wave: 2
depends_on: [12-01, 12-02]
files_modified:
  - monitoring/grafana/provisioning/dashboards/dashboards/ds01_overview.json
  - monitoring/grafana/provisioning/dashboards/dashboards/ds01_historical.json
autonomous: true
requirements: [DASH-01, HIST-02, HIST-03]

must_haves:
  truths:
    - "Admin overview dashboard has stat panels: GPU slots allocated/total, active users, running containers, system GPU utilisation, highest GPU temperature"
    - "Admin overview dashboard has time-series panels for GPU utilisation, memory, power, temperature per device"
    - "Admin overview dashboard has container/user activity panels including enforcement activity from lifecycle metrics"
    - "Admin overview dashboard has system health panels: CPU load, memory, disk, exporter up status"
    - "Historical dashboard shows GPU utilisation heatmap, GPU-hours by user, demand patterns, capacity trend"
    - "Historical dashboard uses recording rules for efficient queries over long time ranges"
  artifacts:
    - path: "monitoring/grafana/provisioning/dashboards/dashboards/ds01_overview.json"
      provides: "Admin operational dashboard with 4 rows of panels"
      min_lines: 500
    - path: "monitoring/grafana/provisioning/dashboards/dashboards/ds01_historical.json"
      provides: "Historical trends and GPU cost attribution dashboard"
      min_lines: 200
  key_links:
    - from: "monitoring/grafana/provisioning/dashboards/dashboards/ds01_overview.json"
      to: "Prometheus datasource"
      via: "datasource uid: prometheus"
      pattern: '"uid":\\s*"prometheus"'
    - from: "monitoring/grafana/provisioning/dashboards/dashboards/ds01_overview.json"
      to: "DS01 recording rules"
      via: "PromQL queries referencing ds01:* metrics"
      pattern: "ds01:"
---

<objective>
Rebuild the admin overview dashboard from scratch with industry-standard layout and create a new historical trends dashboard. The admin dashboard provides real-time operational view; the historical dashboard provides long-term GPU usage trends, cost attribution, and capacity planning data.

Purpose: The existing ds01_overview.json has buggy panels. A methodical rebuild using correct metric names and recording rules ensures every panel displays data.
Output: Complete ds01_overview.json and new ds01_historical.json provisioned via Grafana file provider.
</objective>

<execution_context>
@/home/datasciencelab/.claude/get-shit-done/workflows/execute-plan.md
@/home/datasciencelab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-prometheus-grafana-observability-stack/12-RESEARCH.md
@monitoring/grafana/provisioning/dashboards/dashboards/ds01_overview.json
@monitoring/prometheus/rules/ds01_recording.yml
@.planning/phases/12-prometheus-grafana-observability-stack/12-01-SUMMARY.md
@.planning/phases/12-prometheus-grafana-observability-stack/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild admin overview dashboard (ds01_overview.json)</name>
  <files>monitoring/grafana/provisioning/dashboards/dashboards/ds01_overview.json</files>
  <action>
  Complete rebuild of ds01_overview.json. Use Grafana JSON dashboard model (apiVersion v1, schemaVersion 39 for Grafana 10.x). Datasource UID: `prometheus` (matches provisioned datasource).

  **Dashboard metadata:**
  - uid: `ds01-overview`
  - title: `DS01 Admin Overview`
  - tags: `["ds01", "admin", "gpu"]`
  - timezone: `utc`
  - refresh: `30s`
  - time range: last 1 hour

  **Row 1 — Status at a Glance (stat panels, h=4):**

  Panel 1: GPU Slots Allocated
  - Query: `ds01:system_mig_allocated`
  - Type: stat
  - Thresholds: green (default), yellow >6, red >8

  Panel 2: GPU Slots Free
  - Query: `ds01:system_mig_free`
  - Type: stat
  - Thresholds: green >3, yellow 1-3, red 0

  Panel 3: Active Users
  - Query: `ds01:system_active_users`
  - Type: stat

  Panel 4: Running Containers
  - Query: `ds01:system_containers_running`
  - Type: stat

  Panel 5: System GPU Utilisation (avg)
  - Query: `ds01:system_gpu_utilization_avg`
  - Type: gauge
  - Unit: percent (0-100 scale; DCGM_FI_DEV_GPU_UTIL is 0-100, PROF_GR_ENGINE_ACTIVE is 0-1 but recording rule normalises)
  - Thresholds: green <50, yellow 50-80, red >80

  Panel 6: Highest GPU Temperature
  - Query: `ds01:system_gpu_temp_max`
  - Type: stat
  - Unit: °C
  - Thresholds: green <70, yellow 70-85, red >85

  Panel 7: Active SSH Sessions
  - Query: `ds01_ssh_sessions_total`
  - Type: stat

  **Row 2 — GPU Device Metrics (time series, h=8):**

  Panel 8: GPU Utilisation per Device
  - Query: `ds01:device_utilization`
  - Legend: `{{device}}`
  - Type: timeseries
  - Unit: percent

  Panel 9: GPU Memory % per Device
  - Query: `ds01:device_memory_percent`
  - Legend: `{{device}}`
  - Type: timeseries
  - Unit: percent

  Panel 10: GPU Power (W) per Device
  - Query: `ds01:device_power_watts`
  - Legend: `{{device}}`
  - Type: timeseries
  - Unit: watt

  Panel 11: GPU Temperature (°C) per Device
  - Query: `ds01:device_temperature`
  - Legend: `{{device}}`
  - Type: timeseries
  - Unit: celsius

  **Row 3 — Container & Enforcement Activity (h=8):**

  Panel 12: Containers by User
  - Query: `count by (user) (ds01_gpu_allocated)`
  - Type: bargauge or table
  - Show user name and count of GPU allocations

  Panel 13: Unmanaged GPU Containers
  - Query: `ds01:unmanaged_gpu_containers`
  - Type: stat
  - Thresholds: green 0, red >0
  - Link to detail: show `ds01_unmanaged_gpu_container` info labels

  Panel 14: Enforcement Activity (24h)
  - Query: `ds01_lifecycle_events_total`
  - Type: bargauge
  - Legend: `{{action}}`
  - Shows count of idle kills, runtime kills, warnings etc.

  Panel 15: SSH Sessions by User
  - Query: `ds01_ssh_sessions_active`
  - Legend: `{{user}}`
  - Type: table

  **Row 4 — System Health (h=6):**

  Panel 16: CPU Load Average
  - Queries: `node_load1`, `node_load5`, `node_load15`
  - Type: timeseries
  - Add threshold line at CPU count: `count(node_cpu_seconds_total{mode="idle"})`

  Panel 17: Memory Available
  - Query: `(node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes) * 100`
  - Type: gauge
  - Unit: percent
  - Thresholds: green >30, yellow 10-30, red <10

  Panel 18: Disk Usage
  - Query: `(1 - node_filesystem_avail_bytes{mountpoint="/"} / node_filesystem_size_bytes{mountpoint="/"}) * 100`
  - Type: gauge
  - Unit: percent

  Panel 19: Scrape Target Status
  - Query: `up`
  - Type: table or stat-repeat
  - Show job name + health status (1=up, 0=down)
  - Override: map 1→green "UP", 0→red "DOWN"

  **Build as valid Grafana JSON.** Use `gridPos` with x/y/w/h coordinates. Each row is a `row` panel type with collapsed: false. All panels use datasource `{"type": "prometheus", "uid": "prometheus"}`.

  Ensure all panel IDs are unique integers. Ensure no panel references a non-existent metric — all queries use recording rules (ds01:*) or known exporter metrics (ds01_*, DCGM_FI_*, node_*, container_*).
  </action>
  <verify>
    <automated>cd /opt/ds01-infra && python3 -c "
import json
d = json.load(open('monitoring/grafana/provisioning/dashboards/dashboards/ds01_overview.json'))
assert d.get('uid') == 'ds01-overview', 'wrong uid'
panels = d.get('panels', [])
assert len(panels) >= 15, f'expected 15+ panels, got {len(panels)}'
# Check datasource references
panel_count = 0
for p in panels:
    if p.get('type') == 'row':
        continue
    panel_count += 1
    targets = p.get('targets', [])
    for t in targets:
        ds = t.get('datasource', {})
        if isinstance(ds, dict):
            assert ds.get('uid') == 'prometheus', f'panel {p.get(\"id\")} has wrong datasource'
print(f'Dashboard valid: {panel_count} data panels, uid=ds01-overview')
"</automated>
  </verify>
  <done>Admin overview dashboard fully rebuilt with 4 rows: status-at-a-glance stats, GPU device time series, container/enforcement activity, system health. All panels reference correct metrics. UID is ds01-overview.</done>
</task>

<task type="auto">
  <name>Task 2: Create historical trends dashboard (ds01_historical.json)</name>
  <files>monitoring/grafana/provisioning/dashboards/dashboards/ds01_historical.json</files>
  <action>
  Create new `ds01_historical.json` for long-term trend analysis. This dashboard is designed for weekly/monthly/all-time views, separate from the real-time admin overview.

  **Dashboard metadata:**
  - uid: `ds01-historical`
  - title: `DS01 Historical Trends`
  - tags: `["ds01", "historical", "trends"]`
  - timezone: `utc`
  - refresh: off (manual refresh for historical views)
  - time range: last 7 days (users change as needed)

  **Row 1 — GPU Utilisation Trends (h=8):**

  Panel 1: Hourly GPU Utilisation Trend
  - Query: `ds01:hourly_gpu_util_avg`
  - Type: timeseries
  - Fill opacity: 20
  - Good for week/month views showing usage patterns

  Panel 2: GPU Utilisation Heatmap (time-of-day)
  - Query: `ds01:system_gpu_utilization_avg`
  - Type: heatmap (or timeseries with color-coded fills)
  - Shows usage density by hour of day
  - Use Grafana's built-in heatmap panel type

  **Row 2 — GPU Cost Attribution (h=8):**

  Panel 3: GPU-Hours by User (stacked bar)
  - Query: `increase(ds01:user_gpu_seconds[$__range]) / 3600`
  - Legend: `{{user}}`
  - Type: barchart (stacked)
  - Unit: hours
  - Shows which users consumed how many GPU-hours over selected period

  Panel 4: GPU-Hours Total (stat)
  - Query: `sum(increase(ds01:user_gpu_seconds[$__range])) / 3600`
  - Type: stat
  - Unit: hours
  - Big number showing total GPU-hours consumed

  Panel 5: GPU Efficiency (utilisation-weighted hours vs raw hours)
  - Queries:
    - Raw: `sum(increase(ds01:user_gpu_seconds[$__range])) / 3600`
    - Weighted: `sum(increase(ds01:user_gpu_seconds_weighted[$__range])) / 3600`
  - Type: bargauge or stat comparison
  - Shows how much GPU time was actually utilised vs just allocated

  **Row 3 — Capacity & Demand (h=8):**

  Panel 6: GPU Slot Allocation Over Time
  - Query: `ds01:hourly_mig_allocated_avg`
  - Type: timeseries
  - Fill below line
  - Shows demand patterns (busy/quiet periods)

  Panel 7: Active Users Over Time
  - Query: `ds01:system_active_users`
  - Type: timeseries
  - Shows user activity patterns

  Panel 8: GPU Waste Trend
  - Query: `ds01:gpu_waste_percent`
  - Type: timeseries
  - Thresholds: green <20, yellow 20-50, red >50
  - Shows efficiency trends over time

  **Row 4 — Temperature & Health (h=6):**

  Panel 9: Daily Max GPU Temperature
  - Query: `ds01:daily_gpu_temp_max`
  - Type: timeseries
  - Shows thermal trends (helpful for detecting cooling issues)

  Panel 10: Enforcement Activity Over Time
  - Query: `ds01_lifecycle_events_total` with rate/increase
  - Legend: `{{action}}`
  - Type: timeseries (stacked area)
  - Shows trends in idle kills, runtime kills, warnings

  **Build as valid Grafana JSON.** Same datasource pattern as overview dashboard. Use `$__range` variable for time-range-aware queries (GPU-hours).
  </action>
  <verify>
    <automated>cd /opt/ds01-infra && python3 -c "
import json
d = json.load(open('monitoring/grafana/provisioning/dashboards/dashboards/ds01_historical.json'))
assert d.get('uid') == 'ds01-historical', 'wrong uid'
panels = [p for p in d.get('panels', []) if p.get('type') != 'row']
assert len(panels) >= 8, f'expected 8+ panels, got {len(panels)}'
# Check that GPU-hours query exists
all_exprs = []
for p in d.get('panels', []):
    for t in p.get('targets', []):
        all_exprs.append(t.get('expr', ''))
expr_text = ' '.join(all_exprs)
assert 'user_gpu_seconds' in expr_text, 'missing GPU-hours attribution query'
print(f'Historical dashboard valid: {len(panels)} data panels, uid=ds01-historical')
"</automated>
  </verify>
  <done>Historical trends dashboard created with GPU utilisation trends, GPU-hours cost attribution per user, capacity/demand patterns, temperature trends, and enforcement activity over time. All queries use recording rules for efficient long-range queries.</done>
</task>

</tasks>

<verification>
- Both JSON files are valid JSON and parseable
- ds01_overview.json has uid `ds01-overview`, 15+ panels across 4 rows
- ds01_historical.json has uid `ds01-historical`, 8+ panels across 4 rows
- All panel queries reference metrics from: ds01:* recording rules, ds01_* exporter, DCGM_FI_*, node_*, container_*
- GPU-hours attribution uses `increase(ds01:user_gpu_seconds[$__range]) / 3600`
- Datasource uid is `prometheus` on all panels
</verification>

<success_criteria>
Admin overview dashboard provides real-time operational view with GPU status, container activity, enforcement events, and system health. Historical dashboard provides long-term trend analysis with GPU-hours attribution, capacity patterns, and efficiency metrics. Both dashboards auto-load via Grafana provisioning.
</success_criteria>

<output>
After completion, create `.planning/phases/12-prometheus-grafana-observability-stack/12-04-SUMMARY.md`
</output>
