---
phase: 12-prometheus-grafana-observability-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - monitoring/docker-compose.yaml
  - monitoring/prometheus/prometheus.yml
  - monitoring/alertmanager/alertmanager.yml
  - monitoring/alertmanager/templates/email.tmpl
  - monitoring/grafana/provisioning/dashboards/default.yml
autonomous: true
requirements: [MON-01, MON-03, HIST-01, ALERT-01, ALERT-02]

must_haves:
  truths:
    - "cAdvisor container runs and exposes per-container metrics on port 8080"
    - "Prometheus scrapes cAdvisor, node-exporter (with logind), dcgm-exporter, ds01-exporter, and itself"
    - "Prometheus retention is set to 90 days with 15GB safety cap"
    - "Alertmanager uses msteamsv2_configs with placeholder URL (no email_configs)"
    - "Grafana has anonymous read-only access enabled"
    - "Node exporter collects logind SSH session data"
    - "Prometheus memory limit raised to 4G for 90-day head block"
  artifacts:
    - path: "monitoring/docker-compose.yaml"
      provides: "cAdvisor service, updated Prometheus/Grafana config, node-exporter logind"
      contains: "cadvisor"
    - path: "monitoring/prometheus/prometheus.yml"
      provides: "Scrape config for all 5 targets including cAdvisor"
      contains: "cadvisor"
    - path: "monitoring/alertmanager/alertmanager.yml"
      provides: "Teams-only alert delivery via msteamsv2_configs"
      contains: "msteamsv2_configs"
  key_links:
    - from: "monitoring/prometheus/prometheus.yml"
      to: "cadvisor:8080"
      via: "scrape_configs job_name cadvisor"
      pattern: "cadvisor.*8080"
    - from: "monitoring/alertmanager/alertmanager.yml"
      to: "Teams Power Automate webhook"
      via: "msteamsv2_configs webhook_url"
      pattern: "msteamsv2_configs"
---

<objective>
Update the monitoring stack infrastructure: add cAdvisor for container metrics, extend Prometheus retention to 90 days, migrate Alertmanager from deprecated email+webhook to Teams-only msteamsv2_configs, enable Grafana anonymous read-only access, and enable Node Exporter logind collector for SSH session tracking.

Purpose: The monitoring stack containers and configuration must be correct before dashboards and alert rules can be built on top.
Output: Updated docker-compose.yaml, prometheus.yml, alertmanager.yml — stack ready for `docker-compose up -d`.
</objective>

<execution_context>
@/home/datasciencelab/.claude/get-shit-done/workflows/execute-plan.md
@/home/datasciencelab/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-prometheus-grafana-observability-stack/12-RESEARCH.md
@monitoring/docker-compose.yaml
@monitoring/prometheus/prometheus.yml
@monitoring/alertmanager/alertmanager.yml
@monitoring/grafana/provisioning/dashboards/default.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cAdvisor, update Prometheus retention and memory, enable logind collector</name>
  <files>monitoring/docker-compose.yaml, monitoring/prometheus/prometheus.yml</files>
  <action>
  **docker-compose.yaml changes:**

  1. Add cAdvisor service (insert after dcgm-exporter, before prometheus):
     ```yaml
     cadvisor:
       image: gcr.io/cadvisor/cadvisor:v0.49.1
       container_name: ds01-cadvisor
       restart: unless-stopped
       privileged: true
       ports:
         - "127.0.0.1:8080:8080"
       volumes:
         - /:/rootfs:ro
         - /var/run:/var/run:ro
         - /sys:/sys:ro
         - /var/lib/docker/:/var/lib/docker:ro
         - /dev/disk/:/dev/disk:ro
       devices:
         - /dev/kmsg
       deploy:
         resources:
           limits:
             memory: 256M
             cpus: '0.15'
       healthcheck:
         test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/healthz"]
         interval: 30s
         timeout: 10s
         retries: 3
         start_period: 15s
       logging:
         driver: json-file
         options:
           max-size: "10m"
           max-file: "5"
           labels: "ds01.component"
       networks:
         - ds01-monitoring
       labels:
         - "ds01.monitoring=true"
         - "ds01.component=cadvisor"
         - "ds01.owner=system"
         - "ds01.managed_by=ds01-monitoring-stack"
         - "ds01.container_type=infrastructure"
         - "ds01.protected=true"
         - "ds01.description=Per-container resource metrics (CPU, memory, network, disk I/O)"
     ```

  2. Update Prometheus service:
     - Change retention: `--storage.tsdb.retention.time=90d`
     - Change retention size: `--storage.tsdb.retention.size=15GB`
     - Increase memory limit: `memory: 4G` (from 2G) — needed for 90-day head block
     - Add `cadvisor` to `depends_on` with `condition: service_started`

  3. Update Node Exporter command — add `'--collector.logind'` flag for SSH session tracking. Also need to add `/run/dbus/system_bus_socket` volume mount (required for logind collector to query D-Bus):
     ```yaml
     volumes:
       - /proc:/host/proc:ro
       - /sys:/host/sys:ro
       - /:/rootfs:ro
       - /run/dbus/system_bus_socket:/host/run/dbus/system_bus_socket:ro
     command:
       # ... existing flags ...
       - '--collector.logind'
     ```

  4. Update Grafana environment to enable anonymous read-only access:
     ```yaml
     environment:
       # ... existing vars ...
       - GF_AUTH_ANONYMOUS_ENABLED=true
       - GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
       - GF_AUTH_ANONYMOUS_HIDE_VERSION=true
     ```

  **prometheus.yml changes:**

  5. Add cAdvisor scrape job after the existing node-exporter job:
     ```yaml
     - job_name: 'cadvisor'
       static_configs:
         - targets: ['cadvisor:8080']
           labels:
             ds01_category: 'infrastructure'
             ds01_component: 'cadvisor'
       scrape_interval: 30s
       metric_relabel_configs:
         - source_labels: [__name__]
           regex: 'container_(tasks_state|blkio_io_service_bytes_recursive|blkio_io_serviced_recursive|spec_.*)'
           action: drop
     ```

  6. Add Grafana and Alertmanager self-monitoring scrape jobs (needed for `up{job="grafana"}` and `up{job="alertmanager"}` alert expressions that currently reference non-existent jobs):
     ```yaml
     - job_name: 'grafana'
       static_configs:
         - targets: ['grafana:3000']
           labels:
             ds01_category: 'infrastructure'
             ds01_component: 'grafana'
       scrape_interval: 30s
       metrics_path: /metrics

     - job_name: 'alertmanager'
       static_configs:
         - targets: ['alertmanager:9093']
           labels:
             ds01_category: 'infrastructure'
             ds01_component: 'alertmanager'
       scrape_interval: 30s
       metrics_path: /metrics
     ```
  </action>
  <verify>
    <automated>cd /opt/ds01-infra && python3 -c "import yaml; yaml.safe_load(open('monitoring/docker-compose.yaml')); print('compose valid')" && python3 -c "import yaml; yaml.safe_load(open('monitoring/prometheus/prometheus.yml')); print('prometheus valid')"</automated>
    <manual>Check docker-compose.yaml has cadvisor service, 90d retention, 4G memory, logind flag, anonymous Grafana. Check prometheus.yml has cadvisor, grafana, alertmanager scrape jobs.</manual>
  </verify>
  <done>docker-compose.yaml has cAdvisor service with resource limits, Prometheus has 90d/15GB retention with 4G memory, node-exporter has logind collector enabled with D-Bus mount, Grafana has anonymous viewer access. prometheus.yml has 7 scrape targets: dcgm-exporter, ds01-exporter, node-exporter, prometheus, cadvisor, grafana, alertmanager.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Alertmanager to Teams-only msteamsv2_configs and enable dashboard editing</name>
  <files>monitoring/alertmanager/alertmanager.yml, monitoring/alertmanager/templates/email.tmpl, monitoring/grafana/provisioning/dashboards/default.yml</files>
  <action>
  **alertmanager.yml — full rewrite:**

  1. Remove the entire `global` section (SMTP settings). No email.
  2. Keep `resolve_timeout: 5m` as a top-level global.
  3. Keep existing route tree structure (default receiver + critical child route) but update receiver names.
  4. Keep existing inhibit_rules.
  5. Replace both receivers with msteamsv2_configs. Use a placeholder URL that will be replaced during the Teams webhook checkpoint (Plan 05). The placeholder format makes it obvious it needs replacement:

     ```yaml
     global:
       resolve_timeout: 5m

     route:
       receiver: 'ds01-teams'
       group_by: ['alertname']
       group_wait: 5m
       group_interval: 5m
       repeat_interval: 4h
       routes:
         - match:
             severity: critical
           receiver: 'ds01-teams-critical'
           group_wait: 30s
           repeat_interval: 1h

     inhibit_rules:
       - source_match:
           severity: 'critical'
         target_match:
           severity: 'warning'
         equal: ['alertname']
       - source_match:
           alertname: 'DS01ExporterDown'
         target_match_re:
           alertname: 'DS01.*'
         equal: ['job']

     receivers:
       - name: 'ds01-teams'
         msteamsv2_configs:
           - webhook_url: 'PLACEHOLDER_TEAMS_WEBHOOK_URL'
             send_resolved: true
             title: '[DS01] {{ .Status | toUpper }}: {{ .CommonLabels.alertname }}'
             text: |
               **Severity:** {{ .CommonLabels.severity }}
               {{ range .Alerts }}
               **{{ .Annotations.summary }}**
               {{ .Annotations.description }}
               {{ end }}

       - name: 'ds01-teams-critical'
         msteamsv2_configs:
           - webhook_url: 'PLACEHOLDER_TEAMS_WEBHOOK_URL'
             send_resolved: true
             title: '[DS01 CRITICAL] {{ .CommonLabels.alertname }}'
             text: |
               **CRITICAL ALERT**
               {{ range .Alerts }}
               **{{ .Annotations.summary }}**
               {{ .Annotations.description }}
               {{ end }}
     ```

  6. Remove `templates:` section referencing email templates (no longer needed).
  7. Delete `monitoring/alertmanager/templates/email.tmpl` — email channel removed per user decision.

  **default.yml — enable UI editing temporarily:**

  8. In `monitoring/grafana/provisioning/dashboards/default.yml`, change `allowUiUpdates: false` to `allowUiUpdates: true`. This allows dashboard editing in Grafana UI during the phase, which is essential for iterating on panels. Add a comment noting it should be set back to false after Phase 12 is complete.
  </action>
  <verify>
    <automated>cd /opt/ds01-infra && python3 -c "import yaml; yaml.safe_load(open('monitoring/alertmanager/alertmanager.yml')); print('alertmanager valid')" && python3 -c "
import yaml
am = yaml.safe_load(open('monitoring/alertmanager/alertmanager.yml'))
receivers = {r['name'] for r in am['receivers']}
assert 'ds01-teams' in receivers, 'missing ds01-teams receiver'
assert not any('email_configs' in r for r in am['receivers']), 'email_configs still present'
assert any('msteamsv2_configs' in r for r in am['receivers']), 'msteamsv2_configs missing'
print('alertmanager structure valid')
" && test ! -f monitoring/alertmanager/templates/email.tmpl && echo "email template removed"</automated>
  </verify>
  <done>Alertmanager uses Teams-only delivery via msteamsv2_configs with placeholder URL. No email_configs, no SMTP settings. Email template file deleted. Grafana dashboard provisioner allows UI updates for editing during this phase.</done>
</task>

</tasks>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('monitoring/docker-compose.yaml'))"` — compose is valid YAML
- `python3 -c "import yaml; yaml.safe_load(open('monitoring/prometheus/prometheus.yml'))"` — prometheus config is valid YAML
- `python3 -c "import yaml; yaml.safe_load(open('monitoring/alertmanager/alertmanager.yml'))"` — alertmanager config is valid YAML
- docker-compose.yaml contains `cadvisor` service with `gcr.io/cadvisor/cadvisor:v0.49.1`
- docker-compose.yaml prometheus command contains `--storage.tsdb.retention.time=90d`
- docker-compose.yaml prometheus memory limit is 4G
- docker-compose.yaml node-exporter command contains `--collector.logind`
- docker-compose.yaml grafana env contains `GF_AUTH_ANONYMOUS_ENABLED=true`
- prometheus.yml has scrape jobs for: dcgm-exporter, ds01-exporter, node-exporter, prometheus, cadvisor, grafana, alertmanager
- alertmanager.yml has no `email_configs` and has `msteamsv2_configs`
- No `monitoring/alertmanager/templates/email.tmpl` file
</verification>

<success_criteria>
All monitoring stack configuration files updated and valid. Stack is ready for `docker-compose up -d` with cAdvisor, 90-day retention, Teams-only alerting, anonymous Grafana access, and SSH session tracking via logind.
</success_criteria>

<output>
After completion, create `.planning/phases/12-prometheus-grafana-observability-stack/12-01-SUMMARY.md`
</output>
