name: GPU CI

on:
  schedule:
    - cron: '0 3 * * *'  # Nightly at 03:00 UTC
  workflow_dispatch:

concurrency:
  group: gpu-ci
  cancel-in-progress: true

jobs:
  gpu-tests:
    name: Full test suite (GPU + runtime)
    runs-on: [self-hosted, linux, gpu]
    steps:
      - name: Run runtime tests against live system
        working-directory: /opt/ds01-infra/testing
        run: sudo /home/datasciencelab/anaconda3/bin/python -m pytest . -v --tb=short

      - name: Create issue on failure
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue create \
            --repo "${{ github.repository }}" \
            --title "GPU CI failed ($(date -u +%Y-%m-%d))" \
            --body "Nightly GPU CI failed on $(date -u).

          **Run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

          Check the runtime test logs for details." \
            --label "bug"
