# ================================================
# Per-User Resource Limit Overrides
# ================================================
# /opt/ds01-infra/config/user-overrides.yaml
#
# These settings override group defaults for specific users.
# Use for temporary exceptions, thesis work, special projects, etc.
#
# Format: Use username matching /home/<username>
# - Students: studentID@hertie-school.lan
# - Staff: firstname.lastname@hertie-school.lan
# - Check with: ls /home/ | grep -i "name"
#
# Common override fields:
#   idle_timeout_h: null          # Disable auto-stop on idle
#   max_runtime_h: null           # Disable max runtime limit
#   container_hold_after_stop_h: null  # Don't auto-remove containers
#   max_mig_instances: N        # Custom GPU limit
#   allow_full_gpu: true        # Grant full GPU access
# ================================================

# ----------------------------------------------
# Silke Kaiser - Pre-existing workflow
# In the resarcher group, but this overrides 
# ----------------------------------------------
204214@hertie-school.lan:
  idle_timeout_h: null               # No idle timeout (exempt from auto-stop)
  max_runtime_h: null                # No runtime limit
  container_hold_after_stop_h: null  # Don't auto-remove stopped containers

# ----------------------------------------------
# Add more user overrides below
# ----------------------------------------------
# Example:
241248@hertie-school.lan:
  max_runtime_h: 48

# h.baker@hertie-school.lan:
#   max_mig_instances: 4
#   allow_full_gpu: true
#   # Reason: Special project - approved 2025-XX-XX
